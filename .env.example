NODE_ENV=development
PORT=4000

APP_NAME=AmarPin
APP_URL=http://localhost:3000
API_URL=http://localhost:4000
API_PREFIX=api/v1

# MongoDB
MONGODB_URI=mongodb://127.0.0.1:27017/cinevault

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# Auth / JWT
JWT_ACCESS_SECRET=change-me-to-a-long-random-access-secret-32+
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES_DAYS=90
BCRYPT_ROUNDS=12

# Cookies — production VPS: COOKIE_SECURE=true and COOKIE_DOMAIN=.yourdomain.com
# Localhost dev: APP_URL/API_URL must stay http://localhost — auth cookies auto-relax.
COOKIE_SECURE=false
COOKIE_DOMAIN=
COOKIE_SAME_SITE=lax

# Rate limiting
THROTTLE_TTL_MS=60000
THROTTLE_LIMIT=100
# Auth rate limits apply in production only (development/test skip IP throttling).
AUTH_THROTTLE_TTL_MS=900000
AUTH_THROTTLE_LIMIT=8
LOCKOUT_MAX_ATTEMPTS=5
LOCKOUT_DURATION_MINUTES=15

# Mail (leave SMTP empty to configure in Admin → System → Settings, or log to console in development)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="AmarPin <noreply@localhost>"

# First Super Admin (created only if none exists). Change this password before any public deploy.
BOOTSTRAP_SUPERADMIN_EMAIL=admin@example.com
BOOTSTRAP_SUPERADMIN_PASSWORD=ChangeMe_Admin_123!

MAX_PROFILES_PER_ACCOUNT=5
AVATAR_MAX_BYTES=2097152

# Subscriptions / billing
SUBSCRIPTION_REQUIRE_PAYMENT=true
SUBSCRIPTION_GRACE_DAYS=3
SUBSCRIPTION_DEFAULT_CURRENCY=BDT

# Payments (Stripe Checkout + webhooks). Use PAYMENT_PROVIDER=fake for local UI without Stripe.
# Public production origins reject fake providers and the example webhook secret.
PAYMENT_PROVIDER=fake
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=
PAYMENT_WEBHOOK_SECRET=whsec_test_cinevault_webhook_secret

# Media libraries (absolute directories). Leave empty to configure in Admin → Libraries.
# Deprecated — libraries are created in Admin → Media libraries (not auto-seeded on boot).
# MEDIA_MOVIES_DIR=
# MEDIA_TV_DIR=
FFPROBE_PATH=ffprobe
FFMPEG_PATH=ffmpeg
MEDIA_PROBE=auto

# Optional TMDB key so library scans can pull posters/metadata like Emby.
# Get a free key at https://www.themoviedb.org/settings/api
TMDB_API_KEY=
LIBRARY_AUTO_IMPORT=true
LIBRARY_AUTO_PUBLISH=true

# Streaming — Emby-style on-the-fly transcode (requires ffmpeg on the API host)
# auto = transcode HEVC/EAC3/DTS when the browser cannot play the source directly
STREAM_TRANSCODE=auto
HLS_TRANSCODE_TIMEOUT_MS=180000
HLS_TRANSCODE_SEGMENT_SECONDS=4
HLS_TRANSCODE_INITIAL_SEGMENTS=2
HLS_SEGMENT_SECONDS=4
HLS_SEEK_SEGMENT_SECONDS=2
STREAM_TRANSCODE_PRESET=ultrafast
STREAM_TRANSCODE_CRF=23
STREAM_TRANSCODE_MAX_HEIGHT=1080
STREAM_TRANSCODE_MAX_FPS=0
STREAM_TRANSCODE_ENCODER=libx264
STREAM_TRANSCODE_HWACCEL=auto

# CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000

# License (30-day trial, then require a signed key). Prefer a dedicated signing secret.
# Generate keys: node scripts/generate-license.mjs --days 365 --edition pro
LICENSE_MASTER_SECRET=
# Optional: auto-activate this key on API boot
LICENSE_KEY=
LICENSE_TRIAL_DAYS=30
# LICENSE_SEAL_DIR=
# LICENSE_DISABLED=false

# Web
NEXT_PUBLIC_APP_NAME=AmarPin
NEXT_PUBLIC_API_URL=/api/v1

